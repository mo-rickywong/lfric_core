{# ########################################################################### #}
{# (c) Crown copyright 2024 Met Office. All rights reserved. #}
{# The file LICENCE, distributed with this code, contains details of the terms #}
{# under which the code may be used. #}
{# ########################################################################### #}
{% do LOG.debug("Entered site/common/JediBones/tasks_JediBones.cylc") %}

{% if task_ns.conf_name == "default-C24" %}

    {% do task_dict.update({
        "opt_confs": ["default"],
        "resolution": "C24",
        "configuration_file": "Season1.nml Season2.nml",
        "kgo_checks": [],
	"log_level": "info",
    }) %}

{% elif task_ns.conf_name == "unit_tests" %}

    {% do task_dict.update({
        "placeholder": true,
    }) %}

{% elif task_ns.conf_name == "canned" %}

    {% do task_dict.update({
        "app_name": "canned_test",
        "configuration_file": "Season1.nml Season2.nml",
        "kgo_checks": [],
    }) %}

{% elif task.startswith("build") %}

    {% do task_dict.update({
        "placeholder": true,
    }) %}

{% endif %}

{# Set default values for JediBones tasks #}
{% if task_dict %}
    {% do task_dict.update({"application_dir": "lfric_core/applications/JediBones"}) %}
    {% if "app_name" not in task_dict %}
        {% do task_dict.update({"app_name": "JediBones"}) %}
    {% endif %}
    {% if "kgo_checks" not in task_dict %}
        {% do task_dict.update({"kgo_checks": ["checksum"]}) %}
    {% endif %}
{% endif %}

{% do LOG.debug("Finished in site/common/JediBones/tasks_JediBones.cylc") %}
